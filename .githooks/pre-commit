#!/bin/sh
# Temporary pre-commit stub to allow commits from GitHub Desktop
# Replace this with the real hook by running `node setup-git-hooks.js`
# or executing the repository's setup script.
exit 0

#!/bin/sh
# Pre-commit hook for Gurulo AI project
# Cross-platform version (Windows, Linux, macOS)

# Run Node.js lint-staged script
exec node scripts/run-lint-staged.js
