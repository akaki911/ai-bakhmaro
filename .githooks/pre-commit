
#!/usr/bin/env bash
# Pre-commit hook for Gurulo AI project
# Windows-compatible version

# Skip on Windows if bash not available
if ! command -v bash &> /dev/null; then
    echo "⚠️ Bash not found - skipping pre-commit checks"
    exit 0
fi

# Run linting
npm run lint-staged

exit $?
