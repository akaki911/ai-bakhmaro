
[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile --prefer-offline"
]

[phases.build]
cmds = [
  "echo 'No build step required for AI service'"
]

[start]
cmd = "node server.js"
