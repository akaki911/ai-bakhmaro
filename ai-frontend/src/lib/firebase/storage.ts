import {
  connectStorageEmulator,
  deleteObject,
  getDownloadURL,
  getStorage,
  listAll,
  ref,
  uploadBytes,
  uploadBytesResumable,
  uploadString,
} from 'firebase/storage';

export {
  connectStorageEmulator,
  deleteObject,
  getDownloadURL,
  getStorage,
  listAll,
  ref,
  uploadBytes,
  uploadBytesResumable,
  uploadString,
};

export type {
  FullMetadata,
  ListOptions,
  ListResult,
  StorageReference,
  UploadMetadata,
  UploadResult,
} from 'firebase/storage';
